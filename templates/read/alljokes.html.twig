{% extends 'base.html.twig' %}

{% block title %}Read joke{% endblock %}

{% block body %}
<style>
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="container">
    <div class="row">
        <div class="col-md-10 ml-md-auto">
            <div class="card bg-light mb-3 mt-5" style="width: 800px">
                <div class="card-body">
                    <div class="card-header mb-3">All Jokes</div>
                        <div class="form_group">
                            <div class="col-md-12 mb-3"></div>
{#                            {{ form_start(form) }}#}
{#                            <div class="form_group">#}
{#                                <div class="col-md-12 mb-3">#}
{#                                    {{ form_row(form.id, {'attr': {'class': 'form-control'}}) }}#}
{#                                </div>#}
{#                            </div>#}
{#                            <div class="form-group">#}
{#                                <div class="col-md-8 col-md-offset-4" style="margin-top:5px;">#}
{#                                    <button type="submit" class="btn btn-primary">#}
{#                                        <i class="fa fa-btn fa-user"#}
{#                                                href="{{ [path('app_read_cat', {id: category_id})]}}"#}
{#                                        ></i> Submit#}
{#                                    </button>#}
{#                            {{ form_end(form) }}#}
                                <ul>{% for name in allcategory %}
                                        <li>{{name.name}}</li>
                                        {% for jokes in alljokes %}
                                            {% if name.id == jokes.category %}
                                                <br> {{jokes.joke}}
                                            {%endif%}
                                        {% endfor %}
                                    {% endfor %}
                                </ul>
                        </div>
                </div>
            </div>
        </div>
    </div>
</div>




{% endblock %}
