{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}
{% block body %}
    <style>
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h3 class="text-center">Hello  {{ app.user.name }}!</h3>

    <div class="d-grid gap-2 d-md-block text-center nav-link">
        <a class="btn btn-primary btn-lg" href="{{ path('app_read') }}" role="button">Read</a>
        <a class="btn btn-secondary btn-lg" href="{{ path('app_joke') }}" role="button">Write</a>
    </div>
{% endblock %}
