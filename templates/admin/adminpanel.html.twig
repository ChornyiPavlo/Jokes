{% extends 'base.html.twig' %}

{% block title %}Hello Admin!{% endblock %}

{% block body %}
    <style>
        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
    <h3 class="text-center">Hello {{ app.user.name }}!✅</h3>

    <div class="d-grid gap-2 text-center nav-link">
        <a class="btn btn-dark btn-lg" href="{{ path('app_users') }}" type="button">Users</a>
        <a class="btn btn-dark btn-lg" href="{{ path('app_categories') }}" role="button">Categories</a>
        <a class="btn btn-dark btn-lg" href="{{ path('app_jokes') }}" role="button">All Jokes</a>
        <a class="btn btn-dark btn-lg" href="{{ path('app_jokes_moderate') }}" role="button">New Jokes</a>
    </div>

{% endblock %}